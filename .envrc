# Brain system environment — public directory of brain-kit (split project)
# Run: direnv allow

export BRAIN_GLOBAL=~/Programming/programs/brain
export BRAIN_ROOT=$(pwd)/../brain-kit-private
export BRAIN_PUBLIC=$(pwd)
export BRAIN_CONTEXT=dev
export BRAIN_REPO=brain-kit
