# Brain system environment — fully private repo
# Copy this file to your repo root and fill in the values.
# Run: direnv allow

export BRAIN_GLOBAL=~/Programming/brain
export BRAIN_ROOT=$(pwd)
export BRAIN_PUBLIC=""
export BRAIN_CONTEXT=dev   # dev is the default for programs projects
export BRAIN_REPO=my-repo  # replace with your repo name
